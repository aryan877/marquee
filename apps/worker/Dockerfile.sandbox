FROM node:22-bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends \
    bash \
    ca-certificates \
    curl \
    ffmpeg \
    file \
    git \
    imagemagick \
    jq \
    procps \
    ripgrep \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
